"""My scribe.

Written a piece at a time across the Moving Parts transformers course.
Every function below is one I wrote, except any stretch marked "written
for you", which the course supplies so this file needs nothing but NumPy
to run.

A line like

    # ---- [section:softmax] Chapter 4, Softmax ----

is a section line. The course reads the name in the brackets to find where
each piece starts, so it can run the right tests and tell me which piece a
failure came from. Everything else on the line, and everything between the
lines, is mine to rewrite.
"""

import numpy as np
